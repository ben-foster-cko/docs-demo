post:
  servers:
    - url: //{webhookUrl}
  tags:
    - Webhooks
  summary: Payment voided webhook
  description: |
    Some description
  requestBody:
    content:
      application/json:
        schema:
          $ref: '../schemas/webhooks/v2/payment_voided.json'